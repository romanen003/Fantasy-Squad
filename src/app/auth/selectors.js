export const isAuthSelector = (state) => state.auth.isAuth;
export const isLoadingSelector = (state) => state.auth.isLoading;
export const isFetchedSelector = (state) => state.auth.isFetched;
export const userDataSelector = (state) => state.auth.userData;
